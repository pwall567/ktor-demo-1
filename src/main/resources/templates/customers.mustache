<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Customer List</title>
    <style>
body {
    font-family: Roboto, Lucida, Helvetica, Univers, sans-serif;
}
h1 {
    font-size: 24pt;
    font-weight: bold;
    padding: 20px;
}
.item {
    background-color: #FFE880;
    color: #2A1F55;
    font-size: 16pt;
    padding: 20px;
    margin-bottom: 20px;
    display: flex;
    justify-content: space-around;
}
.total {
    font-size: 16pt;
    padding: 20px;
}
    </style>
  </head>
  <body>
    <h1>
      List of Customers
    </h1>
    {{#list}}
      <div class="item">
        <span>Customer: <b>{{id}}</b></span>
        <span>Name: <b>{{name}}</b></span>
        <span>Balance: <b>${{balance}}</b></span>
      </div>
    {{/list}}
    <div class="total">Total: {{count}}</div>
  </body>
</html>
